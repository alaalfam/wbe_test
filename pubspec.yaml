name: web_test
description: A new Flutter project.
version: 0.0.1+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  build_runner: ^2.1.7
  flutter:
    sdk: flutter
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  hive_generator: ^1.1.2
  provider: ^6.0.2
  vrouter: ^1.2.0

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
